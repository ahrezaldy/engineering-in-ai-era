import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The dev overlay badge sits exactly where the deck's bottom-left chrome is.
  devIndicators: false,
};

export default nextConfig;
